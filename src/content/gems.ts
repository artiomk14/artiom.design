/**
 * Gems tab placeholders — Figma `tab-content` (124:654) has three `content-item`s.
 * Replace with real entries when the work is ready.
 */
export const gems = {
  items: [{ id: 'one' }, { id: 'two' }, { id: 'three' }],
} as const;
